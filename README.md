# Blue-Trigger
BlueTrigger is an Android automation app that triggers custom actions when Bluetooth devices connect or disconnect. Built in Kotlin with foreground services, receivers, and a clean Material 3 UI. Works reliably on Android 12–15.
